/**
 * 3) Crie uma função que recebe dois parâmetros, base e expoente, e retorne a base elevada ao expoente.
 * 
 * 
 * 
 */

function exp(base, exponent) {
    return base ** exponent
}

let b = 5;
let e = 2;
console.log(" - base^exponent = " + exp(b, e));