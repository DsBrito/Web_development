



const solve_calc = (op1, op2) => {
    let add = op1+op2;
    let sub = op1-op2;
    let div = op1/op2;
    let mult = op1*op2;

    console.log("- result ->  add: " + add + " ,sub: " + sub + " ,mult: "+ mult + " ,div: " + div);
}

solve_calc(10, 10);

